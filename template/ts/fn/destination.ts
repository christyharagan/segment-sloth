export async function onTrack(event: SegmentTrackEvent, settings: SegmentSettings) {

}

export async function onIdentify(event: SegmentIdentifyEvent, settings: SegmentSettings) {
  
}

export async function onGroup(event: SegmentGroupEvent, settings: SegmentSettings) {
  
}

export async function onPage(event: SegmentPageEvent, settings: SegmentSettings) {
  
}

export async function onAlias(event: SegmentAliasEvent, settings: SegmentSettings) {
  
}

export async function onScreen(event: SegmentScreenEvent, settings: SegmentSettings) {
  
}